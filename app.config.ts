export default defineAppConfig({
    ui: {
        primary: 'red',   // 👈 your main color (change this to what you want)
        gray: 'neutral',     // subtle grays
        radius: 'lg',        // rounded corners (sm | md | lg | xl | full)
    }
})